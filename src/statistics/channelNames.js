/**
 * Tytuł kanału wyświetlającego liczbę członków.
 * @param count - Liczba członków do wstawienia w tytuł.
 * @return {string}
 */
export const memberCount = count => `Liczba członków: ${count}`

/**
 * Tytuł kanału wyświetlającego ilość filmów.
 * @param count - Ilość członków do wstawienia w tytuł.
 * @return {string}
 */
export const videoCount = count => `Ilość filmów: ${count}`
