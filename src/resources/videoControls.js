import { arrowDown, dislike, like, noEntry } from './emojis'

/**
 * Lista wszystkich przycisków pod filmem, we właściwej kolejności.
 * @type {string[]}
 */
export default [
  like,
  dislike,
  arrowDown,
  noEntry
]
